# Copy this file to .env.local (or set real secrets in CI) and fill values.
# Never commit real secrets to the repo.

# Vite environment variable convention: VITE_*
VITE_MAPBOX_TOKEN=your_mapbox_token_here
